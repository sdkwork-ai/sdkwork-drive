# Drive Sandbox Contracts

Framework-neutral contracts for Drive sandbox pagination, logical entries, directory selection,
and host-injected explorer operations. The package contains no React runtime, SDK construction,
credentials, HTTP transport, provider roots, or host filesystem access.
