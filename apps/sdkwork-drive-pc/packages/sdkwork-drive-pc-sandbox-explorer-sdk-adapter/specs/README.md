# Component Contract

The canonical machine-readable contract is `component.spec.json`. This package is the
Drive-owned adapter between the composed app SDK and the reusable PC sandbox explorer port.
