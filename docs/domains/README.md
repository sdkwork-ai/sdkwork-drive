# Domain Extensions

Register domain-specific documentation directories here when a bounded context needs its own extension layer.

See `../../sdkwork-specs/DOCUMENTATION_SPEC.md` section 2.1 (Extension layer).
