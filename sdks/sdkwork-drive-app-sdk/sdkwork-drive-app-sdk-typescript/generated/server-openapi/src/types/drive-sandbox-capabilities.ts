export interface DriveSandboxCapabilities {
  browse: boolean;
  createDirectory: boolean;
  selectDirectory: boolean;
  readFile: boolean;
  createFile: boolean;
  writeFile: boolean;
  moveEntry: boolean;
  deleteEntry: boolean;
}
